version: "3"

# docker-composes works with services.
services:
  ass-server:
    # build from this directory
    build: ./server
    command: npm start
    volumes:
      - ./server:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 425:425
    environment:
      DATABASE_URL: localhost:3308

  ass-db:
    image: mysql:5.6
    environment:
      MYSQL_ROOT_PASSWORD: ass
      MYSQL_DATABASE: ass
    ports:
      - "3308:3306"

  ass-client:
    build: ./client
    command: npm start
    volumes:
      - ./client:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 420:420

volumes:
  ass-db-volume: {}